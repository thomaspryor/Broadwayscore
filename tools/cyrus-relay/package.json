{
  "name": "cyrus-relay",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Public webhook receiver for Cyrus. Linear POSTs here; the Mac Studio drains the queue outbound, so the Mac needs no inbound public path.",
  "dependencies": {
    "@vercel/blob": "^0.27.0"
  }
}
