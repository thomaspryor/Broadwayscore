# Scheduled workflows intentionally NOT in check-cron-health.yml CRITICAL_CRONS.
#
# A scheduled (cron) workflow must be EITHER in CRITICAL_CRONS (real-time paging for
# user-facing staleness) OR listed here (covered by the daily digest / low-stakes /
# self-healing). audit-cron-health-coverage.js fails CI if a scheduled workflow is in
# neither — so a new cron cannot silently ship with zero monitoring (the gap that hid
# process-feedback.yml being disabled for 15 days, 2026-06-11..26).
#
# To PROMOTE one of these to real-time paging, move it to CRITICAL_CRONS in
# check-cron-health.yml and delete the line here. One filename per line; # = comment.
# Seeded 2026-06-28 with the then-current uncovered set (status quo — no behavior change).

adjudicate-review-queue.yml
aggregator-url-watcher.yml
# Self-healing image-fetch backstop (card #1456): a stale run means at-worst
# a slower drain of the imageless-scored-shows backlog, not user-facing
# staleness worth paging on — its own routeAlert escalation already alerts
# the owner directly when self-heal isn't landing an image after 3 attempts.
audit-imageless-scored-shows.yml
audit-aggregator-coverage.yml
audit-aggregator-gap.yml
audit-bww-rr-attributions.yml
# Digest-noise regression monitor (card #510): self-contained daily check
# that alerts the owner directly via routeAlert() when it finds a violation.
# A stale run means at-worst a 1-day-late detection of a noise regression —
# no user-facing staleness, no paging value.
monitor-scheduled-email-count.yml
# Reverse discovery (missing-show detector): candidates surface in the daily
# digest via health-check.js; a stale run means at-worst 1-day-late candidates.
audit-reverse-discovery.yml
audit-critic-coverage.yml
audit-cross-production-weekly.yml
audit-cross-production.yml
audit-review-quality.yml
audit-touring-contamination.yml
backfill-cast.yml
brand-mention-monitor.yml
check-cookie-health.yml
check-corpus-drift.yml
check-cron-health.yml
check-cwv-health.yml
check-review-count-drift.yml
check-secrets-health.yml
check-seo-health.yml
check-show-freshness.yml
check-url-health.yml
clear-stale-duplicate-of.yml
collect-outlet-reviews.yml
collection-coverage-report.yml
commercial-stale-closures.yml
daily-digest.yml
demo-alias-watchdog.yml
detect-ob-closings.yml
detect-revivals-ibdb.yml
discover-regional-serp-reviews.yml
drain-not-attempted.yml
enrich-ibdb-dates.yml
enrich-reviews.yml
enrich-runtimes.yml
fantasy-weekly.yml
fetch-all-image-formats.yml
fetch-guardian-reviews.yml
finance-ingest.yml
fix-circular-duplicate-pairs.yml
fix-platform-ticket-links.yml
fix-todaytix-links.yml
generate-guide-editorials.yml
generate-related-shows.yml
generate-theater-tips.yml
lighthouse-post-deploy.yml
mirror-data-to-gitlab.yml
mirror-review-texts-to-gitlab.yml
mirror-to-gitlab.yml
newsletter-draft.yml
newsletter-draft-refresh.yml
opening-digest.yml
opening-night-completeness-check.yml
opening-night-reviews.yml
opening-night-stage-alert.yml
poll-loureviews.yml
posthog-monday.yml
process-review-formspree.yml
purge-archives-history.yml
rebuild-fast.yml
recollect-for-scores.yml
reconcile-broadcast-state.yml
recover-wsj-subscriber.yml
reddit-engagement-digest.yml
rediscover-urls.yml
regenerate-tier-configs.yml
review-refresh.yml
rotate-apple-secret.yml
rotate-gitlab-token.yml
scoring-audit.yml
scrape-alltime-grosses.yml
scrape-bww-reviews.yml
scrape-dtli-show-score.yml
scrape-new-aggregators.yml
scrape-nysr.yml
scrape-theatre-reviews.yml
scrape-thestage-roundups.yml
scrape-waltz-costs.yml
scrape-wp-blogs.yml
scraper-cost-report.yml
send-follow-notifications.yml
sentry-triage.yml
snapshot-audience-grades.yml
snapshot-audience-history.yml
snapshot-award-scores.yml
social-post.yml
submit-indexnow.yml
sync-followers.yml
test-ugc-roundtrip.yml
update-broadway-com.yml
update-critic-consensus.yml
update-lbo.yml
update-ltd.yml
update-precursor-awards.yml
update-reddit-sentiment.yml
update-seatplan.yml
update-tony-odds.yml
ux-walkthrough.yml
vercel-build-guard.yml
vercel-demo.yml
vercel-deploy.yml
weekly-affiliate-report.yml
weekly-geo-audit.yml
weekly-integrity.yml
weekly-stubhub-validate.yml
# 2026-07-12: daily duplicateOf heal sweep — self-healing legacy backlog; the
# test.yml same-URL-bylines gate reds if it stops working (digest coverage enough)
dedupe-same-url-bylines.yml
# 2026-07-12: weekly WE newsletter DRAFT dispatcher — human reviews before send;
# a missed draft is noticed at send time, not user-facing staleness
we-newsletter-draft.yml
# 2026-07-13: weekly Show Score parser canary — drift surfaces in daily digest
# via notify-failure; deploy job is push-triggered, not cron. Not user-facing latency.
supabase-functions.yml
# 2026-07-26: weekly newsletter DRAFT score-drift refresh (task #481) — same
# class as we-newsletter-draft.yml above: it refreshes an UNSENT draft that the
# owner reviews before sending, so a missed run is caught at send time and is
# never user-facing. Digest-only; paging here would add noise, not safety.
newsletter-draft-refresh.yml
# 2026-07-31: CI-side delayed re-verification for push-with-retry.sh pushes
# (task #677) — every 20min re-check of recently-pushed SHAs via gh api
# compare. A missed run just means the #668/#619 revert-class detection
# window widens slightly; it's a low-stakes safety net atop push-with-retry.sh's
# own push-time verify, not user-facing staleness. Digest-only.
check-push-ledger.yml
# 2026-08-03: weekly SERP-census recall measurement (Coverage Verdict S1,
# tasks #872/#898/#901). Pure measurement — it gates nothing and changes no
# user-facing data, so a missed run costs a data point, not correctness. It
# also monitors its own absence: health-check.js's "Coverage: SERP census
# recall" check warns when data/audit/census-recall-status.json goes stale
# (tests/unit/health-check-census-recall.test.mjs, "a stale weekly run warns
# that the detector itself has gone blind"), which is strictly better cron
# coverage than CRITICAL_CRONS' generic recency page. Digest-only.
audit-census-recall.yml
# 2026-08-03: weekly seeded adversarial coverage probe (Coverage Verdict S5,
# task #903 — the final sprint). Pure measurement, same posture as
# audit-census-recall.yml: gates nothing, changes no user-facing data, so a
# missed run costs a data point. Also monitors its own absence: health-check.js's
# "Coverage: adversarial probe" check warns when
# data/audit/coverage-adversarial-probe-status.json goes stale
# (tests/unit/health-check-coverage-probe.test.mjs), which is strictly better
# cron coverage than CRITICAL_CRONS' generic recency page. Digest-only.
coverage-adversarial-probe.yml

# 2026-08-09: fails loud on its own push trigger the moment a migration lands
# without being applied (see the workflow's header comment); the weekly cron
# is a secondary drift check for dashboard-side edits, not the primary signal
# a user could notice missing — real-time paging on push already covers the
# case that matters. Digest-only for the weekly leg.
verify-schema.yml
