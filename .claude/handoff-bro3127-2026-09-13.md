# Handoff: BRO-3127 wrap-up tail

You are the crowned successor for BRO-3127 ("P1: check-rebuild-staleness guard lets ONE unrelated show block every rebuild write"). The fix itself is DONE, reviewed (6-reviewer /plan-review + Codex/Claude ship-check with two rounds of real P0 fixes), tested, merged to main, and pushed. Your job is ONLY the verification tail below — do not re-open or re-design the fix.

## What was built (context, no action needed)

- Root cause: `scripts/check-rebuild-staleness.js`'s failure for ONE unrelated show was silently skipping every downstream publish step in `.github/workflows/rebuild-fast.yml` / `rebuild-reviews.yml` (GitHub Actions default `if: success()`), blocking `public/data/shows/*.json` regeneration for every OTHER show in the run.
- Fix: anchor pattern (`id: rebuild` + `always() && steps.rebuild.outcome == 'success'` on every downstream step through "Get new review count" in both workflows), mirroring the existing BRO-2906 pattern. The guard step also now sets a `safe_to_publish` output (true only on a clean run or a SCOPED single-show detection with a populated missing-shows file; false on an unexplained crash) so an unexplained guard crash still blocks everything, same as before.
- A new "Revert public data for shows flagged by staleness guard" step reverts (or removes, if brand-new) just the flagged show's `public/data/shows/{id}.json` after regeneration, so it doesn't republish incomplete state.
- Also fixed: `fetch-guardian-reviews.yml` (the one other caller of the guard — minor gap, not the severe blast-radius bug), and a push-path registration gap (`scripts/check-rebuild-staleness.test.mjs` wasn't reachable by any push-path trigger — fixed in `.github/workflows/test.yml`).
- Commits on main (in order): `9cdd518f193`, `ab4a30445d7`, `694bac4141d` (rebased SHA of `a02429a4eee`), and the push-path fix at `f3ca8e6e809` (rebased SHA `a3b50ea993b`, the CURRENT tip as of writing this).
- New/changed test files: `tests/unit/rebuild-publish-fail-closed.test.mjs`, `scripts/check-rebuild-staleness.test.mjs`, `scripts/lib/workflow-fail-closed-condition.js` (shared helper, also used by `tests/unit/workflow-audit-steps-always.test.mjs` after a refactor). All pass locally as of the last commit.
- Linear plan-review verdict already recorded (`review-gate.mjs --query=record-plan`), ship-check verdict already recorded (`review-gate.mjs --query=record --reviewer=ship-check --result=pass`).
- All three worktrees used this session (`bro-3127-staleness-blast-radius`, `bro-3127-guardian-followup`, `bro-3127-pushpath-fix`) have already been merged to main, pushed, and removed (`git worktree remove --force` + branch deleted). No worktree cleanup needed.

## What you need to do (in order)

1. **Verify CI is green on the current main tip.** Run:
   ```
   git -C /Users/tompryor/Broadwayscore log --oneline -1 origin/main
   RUN_ID=$(gh run list --workflow="Test Suite" --branch main --limit 1 --json databaseId,headSha --jq '.[0].databaseId')
   bash /Users/tompryor/Broadwayscore/scripts/lib/wait-for-run.sh "$RUN_ID" 25
   ```
   (Use `scripts/lib/wait-for-run.sh`, NOT a bare `gh run watch` or a polling loop — this repo's `gh-poll-block.sh` hook will block you after 6 manual `gh run view/list` calls in 10 min.)

2. **If it's still red**, check whether the failures are:
   - **From MY (BRO-3127) changed files** (`.github/workflows/rebuild-fast.yml`, `rebuild-reviews.yml`, `fetch-guardian-reviews.yml`, `test.yml`, `scripts/check-rebuild-staleness.js`, `scripts/lib/rebuild-staleness-guard.js`, `scripts/lib/workflow-fail-closed-condition.js`, `tests/unit/rebuild-publish-fail-closed.test.mjs`, `tests/unit/workflow-audit-steps-always.test.mjs`, `scripts/check-rebuild-staleness.test.mjs`, `tests/unit-test-manifest.txt`) — if so, this genuinely needs fixing; you own it.
   - **From an UNRELATED, already-known-red concurrent regression** — as of run `34770898801` (the run right before the push-path fix), these three failures were confirmed unrelated to BRO-3127, from a concurrent session's BRO-3182 work (`review-file-writer.js`, `review-normalization.js`, `ingest-review-from-url.js`) that merged into main alongside my commits:
     - `tests/unit/find-existing-review-by-url.test.mjs` — "never merges into a wrongProduction file even if URL matched" (genuine logic bug, confirmed via local `node --test`, unrelated file)
     - `scripts/audit-self-contradictory-clear-drained.test.mjs` — "no zero-score-impact self-contradictory-clear remains undrained (BRO-185)" — this is a live-repo-data acceptance probe, may just need the corpus to drain further, not necessarily a code bug
     - A `findGaps` test (name: "findGaps: the real repo reports both shapes and currently has none of either") — file not yet located, needs `grep -rn "findGaps: the real repo reports"` across `tests/` and `scripts/` (including `.ts` files, which weren't checked)
     If the SAME three failures (or their direct causes) are still present on the latest run, they are NOT your responsibility to fix under BRO-3127 — see step 3.
   - **If the push-path fix run itself is still the one showing red** (i.e., you're looking at `34770898801` or `9696cc94361` instead of the run for `a3b50ea993b`/the actual current tip), just wait for the newer run — don't re-diagnose stale data.

3. **File (do not fix) the BRO-3182 main-red finding, if still reproducing.** This was deliberately NOT filed as a Linear card in the original session, because:
   - The owning worktree (`BRO-3182-ingest-slot`) was still `locked` in `git worktree list` as of this session's end — meaning a session is likely still actively working it, and a duplicate card/dispatch risks colliding with in-flight work.
   - Check `git worktree list` yourself first — if `BRO-3182-ingest-slot` is now UNLOCKED (session finished) and the failures above are STILL present on latest main, then it's safe to file: `node scripts/linear-brain.js create "..." --park "..." --notes "..."` with the failing test names/output above as evidence, and a `VERIFY: node --test tests/unit/find-existing-review-by-url.test.mjs` (or whichever survives) as the acceptance command. Do NOT dispatch it yourself unless it's clearly self-contained — this touches scoring/review-write logic you don't have context on.
   - If the worktree is STILL locked, or the failures have since resolved (the other session's own ship-check likely caught them), do nothing — note it in your final report either way.

4. **Report the BRO-3127 outcome to Linear** (this is the actual required close-out for the dispatched ticket — do this regardless of the BRO-3182 tangent above):
   ```
   node scripts/linear-session.js report --issue=BRO-3127 --status=done \
     --summary="Fixed the blast-radius bug: check-rebuild-staleness.js's failure for one show was skipping public/data/shows regeneration for every other show in rebuild-fast.yml/rebuild-reviews.yml (GHA default if:success()). Adopted BRO-2906's anchor pattern (id:rebuild + always()&&steps.rebuild.outcome=='success'), added a safe_to_publish output to distinguish a scoped single-show detection (safe to publish around) from an unexplained guard crash (still blocks everything), and a revert step so the flagged show's own public JSON isn't republished from incomplete state. Also fixed fetch-guardian-reviews.yml's minor gap and a push-path registration bug found by CI itself. Reviewed via /plan-review (6 reviewers) and two rounds of ship-check (Codex + Claude), which each caught real P0s that got fixed. All new/changed tests pass locally." \
     --key-files=".github/workflows/rebuild-fast.yml,.github/workflows/rebuild-reviews.yml,.github/workflows/fetch-guardian-reviews.yml,.github/workflows/test.yml,scripts/check-rebuild-staleness.js,scripts/check-rebuild-staleness.test.mjs,scripts/lib/rebuild-staleness-guard.js,scripts/lib/workflow-fail-closed-condition.js,tests/unit/rebuild-publish-fail-closed.test.mjs,tests/unit/workflow-audit-steps-always.test.mjs,tests/unit-test-manifest.txt" \
     --verification="node --test tests/unit/rebuild-publish-fail-closed.test.mjs tests/unit/workflow-audit-steps-always.test.mjs scripts/check-rebuild-staleness.test.mjs scripts/lib/test-yml-manifest-paths.test.mjs — all pass; actionlint clean on both workflow files; real (non-mocked) execution of check-rebuild-staleness.js against local data confirmed both the healthy path and the missing-show-detection path (including the new formatMissingShowsFile write) work correctly."
   ```
   Adjust `--status` to `blocked` instead if CI is still genuinely red from MY changes at that point (should not happen — verify with step 2 first).

5. Note also: BRO-3224 was already filed this session (Backlog/P2, `VERIFY: owner-judgment`) for a generalized "hard-failing guard step followed by unguarded downstream publish steps" lint auditor — a design task, correctly NOT dispatched. No action needed unless the owner asks about it.

## What NOT to do

- Do not re-review or re-design the BRO-3127 fix itself — it already went through 6 plan reviewers + 2 ship-check rounds.
- Do not dispatch a session onto BRO-3182 without checking the worktree lock state first.
- Do not use `gh run watch` or bare polling loops — use `scripts/lib/wait-for-run.sh`.
- Do not touch `data/opening-night-monitor/`, `data/audit/missed-broadcasts.json`, or similar dirty local-only files in `git status` — these are pre-existing local noise from background processes, unrelated to any of this session's work.
