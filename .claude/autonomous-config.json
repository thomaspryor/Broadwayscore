{
  "//": "Autonomous loop config (owner-approved 2026-07-22): $60 night ceiling / $150 weekly cap. Owner: 'let's just see what it does with $60/night' + 'I doubt we can spend that much for multiple days in a row' — so the night cap is high but clampNightToWeekly (autonomous-budget.js) shrinks later nights when the trailing 7-day ledger spend approaches weeklyUSD. Per-card envelopes raised the same day (S $4 / M $10 per attempt) after the $1.50 S cap killed a completed $2.35 card. L enabled: incremental checkpointing gives it one slice per night, now Opus-sized ($10 max / $5 est) since pickModel forces L's single attempt onto Opus (owner: 'I don't trust Sonnet much' on L/data-destructive cards). Consumed by scripts/autonomous-run.js and scripts/autonomous-email.js; CLI flags override.",
  "nightUSD": 60,
  "weeklyUSD": 150,
  "maxItems": 8,
  "sizes": ["S", "M", "L"],
  "ownerEmail": null,
  "//ownerEmail": "null here (public repo) — the OWNER_EMAIL env var in .env is the real value; config value, if set, overrides",
  "baseUrl": "https://broadwayscorecard.com",
  "linkExpiryHours": 48
}
