{
  "//": "Settings profile for opening-night monitor passes (launched headless by scripts/opening-night-monitor-launch.js via scripts/lib/opening-night-monitor.js -> scripts/lib/claude-cli.js's runClaudeCli, as: claude --print --output-format json --dangerously-skip-permissions --model opus --settings .claude/opening-night-monitor-settings.json, prompt on stdin. Card #650, 2026-07-30: no longer a cmux workspace — cmux refuses launchd-parented process ancestry, so this now runs as one bounded ~15-min headless pass per ~20-min launchd tick instead of one long-lived interactive session; runClaudeCli also hard-refuses fable/mythos for unattended runs (task #459), which is why the model is opus, not the old interactive Fable default. Bypass suppresses permission PROMPTS (an unattended session must never hang on one); these deny rules outrank bypass. Deliberately LOOSER than autonomous-settings.json — the monitor's whole job is pushing data fixes and dispatching pipeline workflows — so only the genuinely irreversible/audience-facing actions are denied: broadcast sends, force pushes, Vercel token ops, and edits to this file / the launcher itself (a session must not widen its own cage). IMPORTANT: prefix deny rules are DEFENSE-IN-DEPTH, not a guarantee — path variants (./scripts, absolute) are covered below but a determined invocation (cd first, node -e, raw curl to Resend) is not expressible here. The primary control for CLAUDE.md rule 17 is the seed prompt's hard ban + the fact that broadcast sends have no legitimate monitor use; owner report emails go through owner-alert-router. gh workflow run stays ALLOWED but rate-capped by ~/.claude/hooks/gh-poll-block.sh (25/hr) — the session's Phase-0 preflight verifies that hook is actually loaded and stops if not.",
  "permissions": {
    "deny": [
      "Bash(node scripts/send-opening-night-broadcast.js:*)",
      "Bash(node ./scripts/send-opening-night-broadcast.js:*)",
      "Bash(node /Users/tompryor/Broadwayscore/scripts/send-opening-night-broadcast.js:*)",
      "Bash(git push --force:*)",
      "Bash(git push -f:*)",
      "Bash(git push origin +:*)",
      "Bash(vercel:*)",
      "Bash(npx vercel:*)",
      "Edit(.claude/opening-night-monitor-settings.json)",
      "Write(.claude/opening-night-monitor-settings.json)",
      "Edit(scripts/opening-night-monitor-launch.js)",
      "Write(scripts/opening-night-monitor-launch.js)"
    ]
  }
}
