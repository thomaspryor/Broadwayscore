name: Notify on failure
description: Send Discord + optional email alert when a workflow fails

inputs:
  title:
    description: Alert title (e.g., "Weekly Grosses Failed")
    required: true
  description:
    description: Optional extra context (action required, likely cause)
    required: false
    default: ''
  severity:
    description: 'Alert severity: "critical" sends Discord (+ email if set). Anything else is silently skipped.'
    required: false
    default: 'low'
  email:
    description: Also send email alert (for critical pipelines)
    required: false
    default: 'false'
  discord_webhook:
    description: Discord webhook URL
    required: true
  resend_api_key:
    description: Resend API key (required if email=true)
    required: false
    default: ''
  owner_email:
    description: Owner email (required if email=true)
    required: false
    default: ''

runs:
  using: composite
  steps:
    - name: Send failure alert
      if: inputs.severity == 'critical'
      uses: actions/github-script@v7
      env:
        DISCORD_WEBHOOK_ALERTS: ${{ inputs.discord_webhook }}
        RESEND_API_KEY: ${{ inputs.resend_api_key }}
        OWNER_EMAIL: ${{ inputs.owner_email }}
      with:
        script: |
          const { sendAlert } = require('./scripts/lib/discord-notify.js');
          const runUrl = `https://github.com/${context.repo.owner}/${context.repo.repo}/actions/runs/${context.runId}`;
          const desc = `${{ inputs.description }}` || 'Check the run logs for details.';
          await sendAlert({
            title: `${{ inputs.title }}`,
            description: desc,
            severity: 'error',
            email: ${{ inputs.email }},
            fields: [
              { name: 'Run', value: `[View logs](${runUrl})` }
            ],
            url: runUrl
          }).catch(e => console.error('Alert notification failed:', e.message));
