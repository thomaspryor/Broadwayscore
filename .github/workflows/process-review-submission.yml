name: Process Review Submission

on:
  issues:
    types: [opened]
  workflow_dispatch:
    inputs:
      issue_number:
        description: 'GitHub Issue number to process'
        required: true
        type: string

jobs:
  validate:
    # Run for workflow_dispatch or new issues with review-submission label
    # (opened only — edited excluded to prevent re-runs on already-processed issues)
    if: |
      github.event_name == 'workflow_dispatch' ||
      contains(github.event.issue.labels.*.name, 'review-submission')
    runs-on: ubuntu-latest
    timeout-minutes: 10
    outputs:
      recommendation: ${{ steps.validate.outputs.recommendation }}
      show_id: ${{ steps.validate.outputs.show_id }}
      review_url: ${{ steps.validate.outputs.review_url }}
      issue_number: ${{ steps.resolve.outputs.issue_number }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v5



      - name: Checkout core data
        uses: ./.github/actions/checkout-core-data
        with:
          token: ${{ secrets.REVIEW_TEXTS_TOKEN }}
      - name: Checkout review-texts (private repo)
        uses: ./.github/actions/checkout-review-texts
        with:
          token: ${{ secrets.REVIEW_TEXTS_TOKEN }}
      - name: Resolve issue context
        id: resolve
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            ISSUE_NUM="${{ github.event.inputs.issue_number }}"
          else
            ISSUE_NUM="${{ github.event.issue.number }}"
          fi
          echo "issue_number=$ISSUE_NUM" >> $GITHUB_OUTPUT

          # Fetch issue body from API (works for both trigger types)
          gh api "repos/${{ github.repository }}/issues/$ISSUE_NUM" --jq '.body' > .issue-body.txt
          echo "Resolved issue #$ISSUE_NUM"

      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: '20'

      - name: Install dependencies
        run: npm ci

      - name: Validate submission
        id: validate
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        run: |
          ISSUE_BODY="$(cat .issue-body.txt)" node scripts/validate-review-submission.js
        # continue-on-error so cleanup steps (comments, labels) still run.
        # Scrape job gates on recommendation=='approve', so crashes are safe.
        continue-on-error: true

      - name: Post validation result
        if: always()
        uses: actions/github-script@v7
        env:
          ISSUE_NUMBER: ${{ steps.resolve.outputs.issue_number }}
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const fs = require('fs');
            const commentFile = '.github-comment.md';
            if (!fs.existsSync(commentFile)) {
              console.log('No validation comment file found');
              return;
            }
            const body = fs.readFileSync(commentFile, 'utf8');
            if (!body.trim()) {
              console.log('Validation comment is empty');
              return;
            }
            const issueNumber = parseInt(process.env.ISSUE_NUMBER);
            await github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: issueNumber,
              body: body
            });

      - name: Add label - Approved
        if: steps.validate.outputs.recommendation == 'approve'
        uses: actions/github-script@v7
        env:
          ISSUE_NUMBER: ${{ steps.resolve.outputs.issue_number }}
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const issueNumber = parseInt(process.env.ISSUE_NUMBER);
            await github.rest.issues.addLabels({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: issueNumber,
              labels: ['validated', 'approved']
            });
            await github.rest.issues.removeLabel({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: issueNumber,
              name: 'needs-validation'
            }).catch(() => {});

      - name: Notify submitter - Approved
        if: steps.validate.outputs.recommendation == 'approve'
        env:
          RESEND_API_KEY: ${{ secrets.RESEND_API_KEY }}
          OWNER_EMAIL: ${{ secrets.OWNER_EMAIL }}
        run: |
          EMAIL=$(grep -A1 '### Submitter Email' .issue-body.txt | tail -1 | xargs)
          if [[ -z "$EMAIL" || "$EMAIL" == "_No response_" ]]; then
            echo "No submitter email — skipping notification"
            exit 0
          fi
          REVIEW_URL=$(grep -A1 '### Review URL' .issue-body.txt | tail -1 | xargs)
          PAYLOAD=$(jq -n \
            --arg from "Broadway Scorecard <noreply@broadwayscorecard.com>" \
            --arg to "$EMAIL" \
            --arg reply_to "$OWNER_EMAIL" \
            --arg subject "Your review submission was approved" \
            --arg text "Great news — the review you submitted to Broadway Scorecard has been approved and will be added to our database shortly.

URL: $REVIEW_URL

Thank you for helping expand our database!" \
            '{from: $from, to: [$to], reply_to: $reply_to, subject: $subject, text: $text}')
          HTTP=$(curl -s -o /dev/null -w "%{http_code}" -X POST https://api.resend.com/emails \
            -H "Authorization: Bearer $RESEND_API_KEY" \
            -H "Content-Type: application/json" \
            -d "$PAYLOAD")
          echo "Resend approved response: $HTTP"
          [[ "$HTTP" =~ ^2 ]] || echo "::warning::Resend approved notification failed (HTTP $HTTP)"

      - name: Add label - Rejected
        if: steps.validate.outputs.recommendation == 'reject'
        uses: actions/github-script@v7
        env:
          ISSUE_NUMBER: ${{ steps.resolve.outputs.issue_number }}
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const issueNumber = parseInt(process.env.ISSUE_NUMBER);
            await github.rest.issues.addLabels({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: issueNumber,
              labels: ['invalid']
            });
            await github.rest.issues.removeLabel({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: issueNumber,
              name: 'needs-validation'
            }).catch(() => {});
            // Close rejected issues
            await github.rest.issues.update({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: issueNumber,
              state: 'closed'
            });

      - name: Notify submitter - Rejected
        if: steps.validate.outputs.recommendation == 'reject'
        env:
          RESEND_API_KEY: ${{ secrets.RESEND_API_KEY }}
          OWNER_EMAIL: ${{ secrets.OWNER_EMAIL }}
        run: |
          EMAIL=$(grep -A1 '### Submitter Email' .issue-body.txt | tail -1 | xargs)
          if [[ -z "$EMAIL" || "$EMAIL" == "_No response_" ]]; then
            echo "No submitter email — skipping notification"
            exit 0
          fi
          REVIEW_URL=$(grep -A1 '### Review URL' .issue-body.txt | tail -1 | xargs)
          PAYLOAD=$(jq -n \
            --arg from "Broadway Scorecard <noreply@broadwayscorecard.com>" \
            --arg to "$EMAIL" \
            --arg reply_to "$OWNER_EMAIL" \
            --arg subject "Your review submission was not approved" \
            --arg text "We reviewed the URL you submitted to Broadway Scorecard but were unable to add it to our database.

URL: $REVIEW_URL

Common reasons: the review is from an outlet we don't currently track, the URL didn't resolve to a review of a Broadway or West End production, or the content didn't meet our scoring criteria. If you think this is an error, you can reply to this email.

Thanks for your submission." \
            '{from: $from, to: [$to], reply_to: $reply_to, subject: $subject, text: $text}')
          HTTP=$(curl -s -o /dev/null -w "%{http_code}" -X POST https://api.resend.com/emails \
            -H "Authorization: Bearer $RESEND_API_KEY" \
            -H "Content-Type: application/json" \
            -d "$PAYLOAD")
          echo "Resend rejected response: $HTTP"
          [[ "$HTTP" =~ ^2 ]] || echo "::warning::Resend rejected notification failed (HTTP $HTTP)"

      - name: Add label - Needs Manual Review
        if: steps.validate.outputs.recommendation == 'needs-manual-review'
        uses: actions/github-script@v7
        env:
          ISSUE_NUMBER: ${{ steps.resolve.outputs.issue_number }}
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const issueNumber = parseInt(process.env.ISSUE_NUMBER);
            await github.rest.issues.addLabels({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: issueNumber,
              labels: ['needs-manual-review']
            });

  scrape-review:
    needs: validate
    # Only run if validation passed
    if: needs.validate.outputs.recommendation == 'approve'
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout repository
        uses: actions/checkout@v5
        with:
          token: ${{ secrets.GITHUB_TOKEN }}



      - name: Checkout core data
        uses: ./.github/actions/checkout-core-data
        with:
          token: ${{ secrets.REVIEW_TEXTS_TOKEN }}
      - name: Checkout review-texts (private repo)
        uses: ./.github/actions/checkout-review-texts
        with:
          token: ${{ secrets.REVIEW_TEXTS_TOKEN }}
      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: '20'

      - name: Install dependencies
        run: npm ci

      - name: Setup Playwright
        uses: ./.github/actions/setup-playwright

      - name: Scrape review
        id: scrape
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          BRIGHTDATA_TOKEN: ${{ secrets.BRIGHTDATA_TOKEN }}
          BRIGHTDATA_ZONE: ${{ secrets.BRIGHTDATA_ZONE }}
          SCRAPINGBEE_API_KEY: ${{ secrets.SCRAPINGBEE_API_KEY }}
          NYT_EMAIL: ${{ secrets.NYT_EMAIL }}
          NYT_PASSWORD: ${{ secrets.NYT_PASSWORD }}
          VULTURE_EMAIL: ${{ secrets.VULTURE_EMAIL }}
          VULTURE_PASSWORD: ${{ secrets.VULTURE_PASSWORD }}
        run: |
          # Get show ID and review URL from validation output
          SHOW_ID="${{ needs.validate.outputs.show_id }}"
          REVIEW_URL="${{ needs.validate.outputs.review_url }}"

          if [ -z "$SHOW_ID" ] || [ -z "$REVIEW_URL" ]; then
            echo "Missing show_id or review_url from validation"
            exit 1
          fi

          # Single-URL ingest. Replaces the previous
          # `collect-review-texts-v2.js --show=X --url=Y` which silently ignored
          # both flags and scanned the full backlog (cancelled at 15-min timeout
          # for issue #309 on 2026-05-03). The new script fetches just the
          # submitted URL, extracts text + byline, resolves the outlet from
          # the URL domain, and runs the same write path as
          # scripts/ingest-manual-review.js.
          node scripts/ingest-review-from-url.js --show="$SHOW_ID" --url="$REVIEW_URL"

      - name: Rebuild reviews.json
        if: success()
        run: node scripts/rebuild-all-reviews.js


      - name: Push review-texts to private repo
        if: always()
        uses: ./.github/actions/push-review-texts
        with:
          token: ${{ secrets.REVIEW_TEXTS_TOKEN }}
          message: "data: Process Review Submission — Commit and push changes"
      - name: Commit and push changes
        if: always()
        env:
          ISSUE_NUMBER: ${{ needs.validate.outputs.issue_number }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          # review-texts now pushed via push-review-texts composite action
          # Core data files pushed to private repo by push-core-data action
          if git diff --staged --quiet; then
            echo "No changes to commit"
          else
            git commit -m "Add review from submission #${ISSUE_NUMBER}"
            bash scripts/lib/push-with-retry.sh
          fi


      - name: Push core data to private repo
        if: always()
        uses: ./.github/actions/push-core-data
        with:
          token: ${{ secrets.REVIEW_TEXTS_TOKEN }}
          message: 'data: Update from process-review-submission'
      - name: Post success comment
        if: success()
        uses: actions/github-script@v7
        env:
          ISSUE_NUMBER: ${{ needs.validate.outputs.issue_number }}
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const issueNumber = parseInt(process.env.ISSUE_NUMBER);
            const now = new Date().toISOString();
            const body = '## ✅ Review Successfully Added!\n\n' +
              'The review has been scraped and added to our database. ' +
              'It will appear on the site within a few minutes after the next deployment.\n\n' +
              'Thank you for your contribution to Broadway Scorecard! 🎭\n\n' +
              '---\n' +
              '*Added by automated system • ' + now + '*';
            await github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: issueNumber,
              body: body
            });

            // Close the issue
            await github.rest.issues.update({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: issueNumber,
              state: 'closed',
              state_reason: 'completed'
            });

      - name: Notify on failure
        if: failure()
        uses: ./.github/actions/notify-failure
        with:
          title: 'Process Review Submission Failed'
          discord_webhook: ${{ secrets.DISCORD_WEBHOOK_ALERTS }}
          resend_api_key: ${{ secrets.RESEND_API_KEY }}
          owner_email: ${{ secrets.OWNER_EMAIL }}

      - name: Post failure comment
        if: failure()
        uses: actions/github-script@v7
        env:
          ISSUE_NUMBER: ${{ needs.validate.outputs.issue_number }}
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const issueNumber = parseInt(process.env.ISSUE_NUMBER);
            const now = new Date().toISOString();
            const body = '## ⚠️ Scraping Failed\n\n' +
              'The review validation passed, but we encountered an error while scraping the review content.\n\n' +
              'A maintainer will investigate and manually add the review if possible.\n\n' +
              '---\n' +
              '*Error occurred at ' + now + '*';
            await github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: issueNumber,
              body: body
            });

            // Add needs-manual-review label
            await github.rest.issues.addLabels({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: issueNumber,
              labels: ['scraping-failed']
            });
