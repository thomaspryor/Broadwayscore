---
name: project-linear-migration-decision
description: "Owner rejected repair of the Notion+cmux session system (2026-08-11); replacement plan (Linear + Cyrus + PR-gated Done) delivered, awaiting go/no-go — never propose \"the system is good, one more fix\" again"
metadata: 
  node_type: memory
  type: project
  originSessionId: 76cfeb74-28a2-45d0-bc25-b0c25b19e227
  modified: 2026-08-26T17:51:26.700Z
---

2026-08-11: After ~35 sessions proposing incremental fixes, the owner explicitly rejected repair of the Notion + cmux + dispatcher session system and asked for a best-in-class replacement. An evidence audit (verified from raw ledgers) found: 39% of "Done" cards fail next-day recheck (56/143), 30% workspace dead-launch rate over 7d (60% on 8/11, worsening), 48/420 dispatches abandoned, digest-autofix 4/17 success, ~94% of token spend was context replay (8/2 transcript audit), ~1/3 of open backlog is self-referential system cards.

**Why:** The failure is structural (self-reported Done, invisible local executor, self-generated workload, self-verified repairs). The owner is DONE with "your system is actually quite good, just fix X" answers — that framing is explicitly banned.

**How to apply:** Never propose repairing/tuning the cmux fleet, dispatcher, digest-autofix, or hook-gate layer as a solution to session-system pain. The plan is v2 (six-reviewer critique passed, verdict recorded in .claude/review-verdicts.jsonl 2026-08-11): Linear sole board; HOSTED runners (NOT Mac Studio) — Cyrus for Claude Code + Codex's Linear integration (owner wants Claude/Codex split to relieve Max limits); runners code-only, never write data files; alerts repoint at the routeAlert() chokepoint in scripts/lib/owner-alert-router.js (one line, 85 call sites) via a single scripts/lib/linear.js with injectable client; Done = merged+deployed+post-deploy-check for code, verification-command + scheduled recheck for ops; branch protection built from scratch WITH include-administrators; pilot-first ramp (5 hand-made issues + 1 delegation per runner + phone intervention BEFORE any import). Timeline 3-4 part-time weeks, NOT "2-3 days". Key measured facts: reminder overhead only partially fixed (still ~85-100K tokens/injection vs 15K target, verification tasks #891/#908 never ran); task #383 auto-redispatched 56x in 5 days; 223 workflows; 31 Notion-writing scripts; 169-file coupling surface. Full plan: ~/Documents/claude-outputs/retire-the-fleet-2026-08-11.md + artifact https://claude.ai/code/artifact/d1dbb044-7f4b-41c9-b7f5-b6d005b0865e. PHASE 0 RAN 2026-08-11/12: owner created Linear workspace "broadway-scorecard" (team BRO, API key in gitignored .env as LINEAR_API_KEY); Cyrus (Team Cloud trial, converts to $120/mo ~8/18 — decision card BRO-10 due 8/17) + Codex connected as workspace agents. RESULTS: Cyrus delegation BRO-7 succeeded end-to-end (PR #571, supervisor caught unregistered-test gap, merged); supervisor-as-runner BRO-8 succeeded (PR #572 merged); Codex NEVER started (session stale despite 3 prompts) — owner must check ChatGPT/Codex cloud environment setup for the repo before the Codex lane counts as verified. Gotchas: Linear delegation sets `delegate` field (assignee stays human); agents need a kickoff comment in the session thread, assignment alone doesn't start them; CI only runs tests literally listed in tests/unit-test-manifest.txt — check agent PRs for this. 2 of 4 pilot issues were already-fixed stale cards — curate hard during Phase 1 import. INTAKE CONVENTION (owner rule 2026-08-12): the owner NEVER performs Linear mechanics. When the owner describes any new initiative/idea/project in plain words, in any session, the session immediately: (1) creates a Linear project (state "started", teamId 573224e1-99a1-4f01-8119-9acb6d85d407) with issues broken out per phase, (2) delegates delegable pieces (Cyrus; Codex when working) WITH a kickoff comment in the session thread — delegation alone does not start agents, (3) replies with the single project link. Same for smaller asks → single issue. Never instruct the owner how to create projects/issues themselves. Related: [[project_session_system_v2_overhaul]] (superseded if migration completes).

**2026-08-19 (BRO-120):** Another already-fixed stale card slipped through — BRO-120 ("Email gate 0.6% conversion — 68% dismiss") was a duplicate of task #586 (merged 2026-08-10, PR #586), same audit numbers verbatim. 3rd confirmed instance of this pattern (2 of 4 pilot issues + this one). When a dispatched Linear card describes a metric/audit finding, grep git log for the same numbers/keywords BEFORE implementing — the fix may already be shipped and tested.

**2026-08-20 (BRO-140 recheck):** Confirmed live: task #854/S1's fix (archiving completed tasks out of `~/.claude/tasks/broadwayscore/`, 860→358 lines / ~9K tokens) never touched the actual `task_reminder` transcript injection driving the 85-100K token figure — `scripts/lib/task-store-archive.js` has zero references to `task_reminder`, and real 8/3 transcripts show 803 `task_reminder` injections averaging ~95,636 tokens each (max ~184,266), with the same mechanism still averaging ~45,592 tokens in the most recent measurable sample (since 8/18, now rare — 18/401 sessions). Whatever generates `attachment.type === "task_reminder"` (likely a cmux-side fleet-wide list, not this repo's local per-project store) was never the thing S1 fixed. If a future session reconsiders reviving/repairing the local task-store archiver as a lever on context cost, this is why it won't move the number — the injection it targets isn't the expensive one. blocks/session also rechecked: 2.17 avg on 8/3 (target <1.0, baseline 4.6) — improved but still over target; 1.31 avg in the most recent window. Full method: `scripts/lib/session-metrics.js` + `scripts/session-metrics-recheck.js` (PR #612).

**2026-08-19 (BRO-24), 4th instance:** "NYC rollout: anchored bands + V6 decompression (Hamilton 91→95)" — an auto-enriched card whose body cited two Notion card IDs directly. Both were already Done, closed 2026-08-18, with a DIFFERENT real outcome than the title's aspirational number (actual: Hamilton 91→91, the 95 was the pre-rollout simulation's prediction, never the measured result). The card's literal acceptance criterion (a `test -f` on a specific new file path) also didn't match how the codebase actually implements the feature. General rule now confirmed 4x: before implementing a Linear card literally, if the description names a Notion card ID, `node scripts/notion-brain.js get <id>` it first — a Done/closed referenced card means the work may already exist and the Linear AC may be stale or fabricated relative to the real implementation. 53 of 324 open Linear issues carry the auto-enriched label as of 2026-08-19 (unaudited); roadmap card 3c1637c5-416f-818f-acfe-ebd7555e633b tracks a systematic sweep.

**2026-08-20 (BRO-141), 5th instance — a variant, not just stale/Done:** the linked Notion card (3b1637c5) was still open (Not started), so the Done/closed check above wouldn't have caught it — but its Notes/Outcome had evolved past the Linear issue text: priority downgraded P1→P2 with measured yield data, the underlying code fix already shipped, and the real acceptance criteria (a proven-zero-sweep guard) was completely different from what the Linear issue literally asked for (a generalization test). Rule holds even when the card isn't closed: always `get` the linked Notion card and read its FULL current Notes/Outcome, not just check its status — a card can drift arbitrarily far from the dispatching issue's original snapshot while still open.

**2026-08-26 (BRO-385), 6th instance — the card's own quantitative claim, no linked reference needed:** "close ~90 self-referential cards" (from "~1/3 of open backlog is self-referential" in this file's 8/11 audit). The number checked out (classifyNoise's fleet_selfref+bsc_daily categories = 90 open cards), but "self-referential" ≠ "safe to bulk-close": fetching full Notes/Outcome showed ~half carry a pending RECHECK-AFTER for an already-shipped, CI-proven fix, or a substantial written Outcome — real work, not noise. classifyNoise was built to decide Notion→Linear import eligibility, not delete-safety, and got informally reused for the latter without re-validating the assumption. A naive title-only-match first pass actually closed a live P0 pending-recheck card before this was caught from the Outcome content and reverted. Rule: when an auto-enriched card's number comes from a classifier/filter built for a DIFFERENT decision, re-derive safety from the actual card content before acting at that classifier's scale — don't trust the count as a proxy for "safe."
