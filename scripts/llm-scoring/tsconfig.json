{
  "//": "Focused tsc config for CI's TypeScript Check gate. Inherits compiler",
  "//1": "options from scripts/tsconfig.json but restricts include to llm-scoring/",
  "//2": "so the gate is green from a clean baseline (the parent's whole-tree",
  "//3": "check has 86 pre-existing rootDir errors in other scripts unrelated to",
  "//4": "this codepath). Extending scope to other directories requires fixing",
  "//5": "their rootDir issues first.",
  "//6": "Added 2026-05-16 after evaluate.ts shipped a 12-error consumer mismatch",
  "//7": "that no existing CI gate caught.",
  "extends": "../tsconfig.json",
  "include": ["./**/*.ts"],
  "exclude": ["node_modules", "dist"]
}
