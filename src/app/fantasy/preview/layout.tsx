/**
 * Fantasy League BTC-style preview — standalone layout (no site chrome).
 * Mirrors beat-the-critics/layout.tsx. Body class `btc-standalone` hides nav/footer.
 */
export default function FantasyPreviewLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
