/**
 * Beat the Critics has its own layout — no site nav, no footer.
 * This is a standalone co-branded experience (TodayTix x Broadway Scorecard).
 */
export default function BeatTheCriticsLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
